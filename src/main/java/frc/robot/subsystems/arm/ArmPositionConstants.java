package frc.robot.subsystems.arm;

import static frc.robot.subsystems.arm.elevator.ElevatorConstants.*;
import static frc.robot.subsystems.arm.wrist.WristConstants.*;

public class ArmPositionConstants {
    public static final ArmPosition ZEROED = new ArmPosition(ZEROED_HEIGHT, ZEROED_ANGLE);
    public static final ArmPosition DEFAULT = new ArmPosition(DEFAULT_HEIGHT, DEFAULT_ANGLE);
}
